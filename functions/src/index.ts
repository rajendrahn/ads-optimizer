// Scheduled sync entrypoints land here starting at B1 (see IMPLEMENTATION_PLAN.md).
// A1 only needs this package to exist and build so the Functions emulator has something
// valid to load — no business logic belongs in this file yet.
export {};
